# 将浮点数转化为整数
x = 3.14
num_x = int(x)
print(type(num_x), num_x)

# 任何语句都可以通过str()这个语句转化为字符串
# 但是想要将字符串转化为数字则需要确认字符串中是否都为数字


# 标识符命名：不建议使用中文；不能以数字开头；不能占用关键字；区分大小写；

# 字符串定义有三种写法：1、单引号定义法；2、双引号定义法；3、三引号定义法
# 三引号定义法的写法和多行注释的写法是一样的，区分的方法；看三引号定义前面是否有变量被赋值接收，有则是三引号定义字符串，否则为多行注释

